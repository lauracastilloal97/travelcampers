<form method="post" action="{{ path('campers_delete', {'id': camper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ camper.id) }}">
    <button class="btn">Delete</button>
</form>
