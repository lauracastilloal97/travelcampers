<form method="post" action="{{ path('rute_delete', {'id': rute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rute.id) }}">
    <button class="btn">Borrar</button>
</form>
